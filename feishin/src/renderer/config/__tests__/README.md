# Runtime Config Tests

Add unit tests here for:

- boolean coercion from `settings.js`
- required internal server fields
- safe fallback defaults for locked deployments
- Telegram/mobile/Desktop runtime decisions
